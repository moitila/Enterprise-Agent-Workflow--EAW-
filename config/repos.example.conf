# Example repos.conf
# Format: key|path|role (role optional; default=target)
# role accepts: target|infra
# Use absolute or relative paths. Replace with your local repos before running CLI.
local-main|.|target
platform-infra|../platform|infra
