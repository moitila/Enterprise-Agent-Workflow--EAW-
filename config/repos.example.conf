# Example repos.conf
# Format: key|path|role(optional)
# role values: target (default), infra
# Use absolute or relative paths. Replace with your local repos before running CLI.
local-main|.|target
shared-infra|../infra|infra
